# Programing 수업 소스 코드
## 02_counter
Vanilla HTML, CSS, JavaScript로 만든 Counter
---
## 20260324 React 시작
```shell
npm create vite@latest .
```
## CounterApp
  * `setCount();`
  * `setCount(() => set함수());`
  * `setCount(() => set함수(이전state => 이전state + 1));`
## Todo List App